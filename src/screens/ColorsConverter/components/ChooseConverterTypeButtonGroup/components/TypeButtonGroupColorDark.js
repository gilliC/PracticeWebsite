import styled from 'styled-components';
import { MainButtonGroup } from "src/commonComponents/common_components";

export const TypeButtonGroupColorDark = styled(MainButtonGroup)`
margin-top: 30px;
`;